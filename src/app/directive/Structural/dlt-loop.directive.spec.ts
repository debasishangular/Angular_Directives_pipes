import { DltLoopDirective } from './dlt-loop.directive';

describe('DltLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new DltLoopDirective();
    expect(directive).toBeTruthy();
  });
});
